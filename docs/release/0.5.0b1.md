# DeepUplift 0.5.0b1 Beta Release

Release status: `READY_BUT_PYPI_BLOCKED`.

## Release record

- Release commit: `88d8634edb925a2fa6eb6a96bd38794c56efda4f` (protected `main` merge).
- Tag: not created; the release gate stopped before tag creation.
- GitHub Release: not created; the release gate stopped before publication.
- TestPyPI: build/tests passed, OIDC upload blocked by `invalid-publisher`.
- PyPI: not attempted; TestPyPI Trusted Publisher is not configured.
- Benchmark run: `20260903T175621Z-42`.
- Benchmark evidence commit: `40b32dfdab84917ed3ebb1089a3ba91859faf545`.
- Quality CI: `33895250987` passed on merged `main` (Python 3.10, 3.11, and 3.12).
- Optional CI: `33895275080` passed EconML, CausalML Tree/RF, and SkLift interface-only validation.

## Current blocker

The TestPyPI Trusted Publisher for this repository/environment is not configured.
The OIDC action returned `invalid-publisher`; no long-lived token was used or
stored. Configure the GitHub repository/workflow publisher for
`publish-testpypi.yml` with environment `testpypi`, then rerun that workflow.
Configure the analogous PyPI publisher for `release.yml` with environment `pypi`
before the final GitHub Release publication.

## Optional backend status

- EconML `CausalForestDML`: real fit/predict/metric smoke passed.
- CausalML `UpliftTree`: real fit/predict/metric smoke passed.
- CausalML `UpliftRandomForest`: real fit/predict/metric smoke passed.
- scikit-uplift: interface-only and does not block this beta release.

## Known limitations

- Multi-treatment optimization remains a greedy/reference strategy.
- Continuous treatment remains experimental and offline-only.
- Observational estimates depend on conditional ignorability and positivity.
- Offline policy value does not prove online lift.
- Deep neural uplift models remain research/legacy compatibility paths.
