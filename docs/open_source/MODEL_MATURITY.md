# Model maturity

`STABLE` means the native contract is runnable in core CI. `OPTIONAL` means an external dependency is required. `EXPERIMENTAL` means a reference boundary is runnable but not a production claim. `LEGACY` means compatibility code is retained. `INTERFACE_ONLY` means no runnable estimator is claimed; `build_model()` fails explicitly instead of silently substituting another backend.
